import SensorSwitch from './SensorSwitch'

test('exports a valid component', () => {
  expect(SensorSwitch).toBeAComponent()
})

// test('renders the text "SensorSwitch"', () => {
//   const { element } = shallowMount(SensorSwitch)
//   expect(element.textContent.trim()).toBe('SensorSwitch')
// })

// test('adds a "hello" class on the root element', () => {
//   const { element } = shallowMount(SensorSwitch)
//   expect(element.classList.contains('hello')).toBe(true)
// })
