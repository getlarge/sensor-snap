import SensorTime from './SensorTime'

test('exports a valid component', () => {
  expect(SensorTime).toBeAComponent()
})

// test('renders the text "SensorTime"', () => {
//   const { element } = shallowMount(SensorTime)
//   expect(element.textContent.trim()).toBe('SensorTime')
// })

// test('adds a "hello" class on the root element', () => {
//   const { element } = shallowMount(SensorTime)
//   expect(element.classList.contains('hello')).toBe(true)
// })
