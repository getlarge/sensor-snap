import SensorLevel from './SensorLevel'

test('exports a valid component', () => {
  expect(SensorLevel).toBeAComponent()
})

// test('renders the text "SensorLevel"', () => {
//   const { element } = shallowMount(SensorLevel)
//   expect(element.textContent.trim()).toBe('SensorLevel')
// })

// test('adds a "hello" class on the root element', () => {
//   const { element } = shallowMount(SensorLevel)
//   expect(element.classList.contains('hello')).toBe(true)
// })
