import SensorSnap from './SensorSnap'

test('exports a valid component', () => {
  expect(SensorSnap).toBeAComponent()
})

// test('renders the text "SensorSnap"', () => {
//   const { element } = shallowMount(SensorSnap)
//   expect(element.textContent.trim()).toBe('SensorSnap')
// })

// test('adds a "hello" class on the root element', () => {
//   const { element } = shallowMount(SensorSnap)
//   expect(element.classList.contains('hello')).toBe(true)
// })
